{
  "name": "pr-buddy",
  "version": "0.11.0",
  "private": true,
  "description": "Dev harness for the PR Buddy extension (tests only \u2014 the extension itself has no build step).",
  "scripts": {
    "test": "node --test 'tests/**/*.test.js'"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}